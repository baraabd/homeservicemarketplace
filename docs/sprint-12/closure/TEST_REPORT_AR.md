# تقرير الاختبارات — إغلاق Sprint 12

**المصدر:** `sprint12/closure-20260920` @ `65eeff1` فوق `develop` `8cad728`
**الشجرة:** `C:\Users\mohab\hsm-s12` · **التاريخ:** 2026-09-20
**الأدوات:** Node v20.18.1 · pnpm 10.32.1 (`--frozen-lockfile`)

كل الأرقام أدناه من تشغيل فعلي في هذه الجلسة. لا يوجد رقم منقول عن تقرير سابق.

---

## 1. البوابات الثابتة والبناء

| البوابة            | الأمر                                                     |  rc | النتيجة                                |
| ------------------ | --------------------------------------------------------- | --: | -------------------------------------- |
| تثبيت التبعيات     | `pnpm install --frozen-lockfile`                          |   0 | PASS                                   |
| بناء العقود        | ضمن `build:deps`                                          |   0 | PASS                                   |
| توليد Prisma       | `pnpm --filter @homeservicemarketplace/database generate` |   0 | PASS                                   |
| فحص أنواع الويب    | `pnpm --filter @homeservicemarketplace/web typecheck`     |   0 | PASS                                   |
| فحص أنواع E2E      | `pnpm --filter @homeservicemarketplace/web typecheck:e2e` |   0 | PASS                                   |
| Lint الويب         | `pnpm --filter @homeservicemarketplace/web lint`          |   0 | PASS — **0 أخطاء، 34 تحذيرًا** (سابقة) |
| بناء الويب للإنتاج | `VITE_API_URL=… pnpm --filter …/web build`                |   0 | PASS — `index-_PjgojJC.js`             |
| بناء الـAPI        | `pnpm --filter @homeservicemarketplace/api build`         |   0 | PASS                                   |

**ملاحظة نزاهة:** أول تشغيل لبناء الـAPI فشل بـ`rc=2`
(`Cannot find module '../generated/prisma'`). السبب أن الشجرة جديدة ولم يُولَّد
عميل Prisma بعد؛ ليس عيبًا في الكود. بعد `generate` نجح البناء. سُجِّل هنا بدل
إخفائه.

---

## 2. اختبارات الوحدات والمسار

| الحزمة                   | الأمر                                                                                                  |  rc | النتيجة                        |
| ------------------------ | ------------------------------------------------------------------------------------------------------ | --: | ------------------------------ |
| خط الأساس قبل التعديل    | `vitest run src/app/features/disputes …`                                                               |   0 | **37/37** نجحت، 6 ملفات        |
| تبويبات الأدمن الجديدة   | `vitest run …/admin-workspace-tabs.test.tsx`                                                           |   0 | **12/12** نجحت                 |
| انحدار موسّع بعد التعديل | `vitest run src/app/features/disputes src/app/features/admin-provider-review src/app/components/admin` |   0 | **217/217** نجحت، **32 ملفًا** |

### 2.1 ما تغطيه الاختبارات الاثنا عشر الجديدة

تُشغَّل على **المسار الحقيقي** (`resolveAdminRoute` + `AdminRouteContent`)، لا على
المكوّن معزولًا، وببيانات HTTP حتمية عبر `axios-mock-adapter`:

| #     | السيناريو                                                                                                                                  | النتيجة |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| 1     | ستة تبويبات `role="tab"`، **لوحة واحدة** `role="tabpanel"`، الوسم `data-dispute-workspace-layout="tabbed-v1"`، واختفاء صف الروابط `#case-` | PASS    |
| 2     | اللوحات غير النشطة **مركّبة لكن مخفية** (`tabindex="-1"`) — وهو ما يحفظ النص غير المُرسَل                                                  | PASS    |
| 3     | اختيار التبويب من الـURL مع **حفظ `state` و`cursor`** للعودة إلى صفحة الطابور                                                              | PASS    |
| 4     | الروابط القديمة `#case-solutions` ما زالت تختار لوحتها وتركّز عليها                                                                        | PASS    |
| 5     | تنقّل بلوحة المفاتيح وحدها (`ArrowRight`، `End`)                                                                                           | PASS    |
| 6     | أزرار «القسم السابق/التالي»                                                                                                                | PASS    |
| 7     | **تصفّح كل التبويبات لا يُرسل أي أمر** (`post/patch/put/delete` = 0) ولا يعرض إجراءً لم يسمح به الخادم                                     | PASS    |
| 8     | العربية و`dir="rtl"` على المسار الحقيقي                                                                                                    | PASS    |
| 9     | **واجهة المشارك تبقى صفحة مكدّسة بلا تبويبات**                                                                                             | PASS    |
| 10–12 | تحليل الـURL: قيم غير موثوقة تسقط إلى التبويب الأول، الـhash يتقدّم على الاستعلام، ولا يُفقد أي مُعامل                                     | PASS    |

**تشخيص فشل حقيقي جرى إصلاحه:** سقطت 4 اختبارات أولًا. السبب أن
`fireEvent.click` المجرّد لا يُفعِّل اختيار Radix (يختار عند `mousedown`/`focus`)،
وأن أحد التأكيدات قرأ الصفحة قبل وصول البيانات. أُصلح الاختبار باتباع العُرف
القائم في المستودع نفسه (`fireEvent.mouseDown(el, { button: 0, ctrlKey: false })`
في `ReviewTaskTabs.test.tsx`) وبانتظار المرجع المحمَّل. **لم يُضعَّف أي تأكيد ولم
تُرفع أي مهلة.**

---

## 3. أدلة زمن التشغيل الفعلية

API معزول على `:4022`، والويب المبني مخدوم على `:4190`، مقابل
Postgres/Redis/Mailpit على منافذ عابرة. **المنفذ 4000 لم يُمس** (تملكه حاوية
`docker-api-1`).

### 3.1 سلسلة هوية المصدر ← البناء ← المخدوم

| الطبقة           | الدليل                                                         |
| ---------------- | -------------------------------------------------------------- |
| المصدر           | `65eeff1` على `sprint12/closure-20260920`                      |
| الحزمة           | `index-_PjgojJC.js` — تحوي الوسم `tabbed-v1`                   |
| المخدوم = المبني | sha1 `408773c433e2e196879a3e38c1054431524cadf5` **متطابق**     |
| قاعدة البيانات   | `hsm_phase2_it` @ `127.0.0.1:59877` — 60 ترحيلًا، `up to date` |

### 3.2 سلوك التفويض من الخادم الحقيقي

| الطلب                                             | النتيجة                                                                          |
| ------------------------------------------------- | -------------------------------------------------------------------------------- |
| `GET /v1/admin/dispute-workspaces` بلا اعتماد     | **401**                                                                          |
| `GET /v1/admin/dispute-workspaces/:id` بلا اعتماد | **401**                                                                          |
| `GET /v1/me/disputes/:id/workspace` بلا اعتماد    | **401**                                                                          |
| `GET /admin/disputes/:id` من خادم الويب           | **200** (توجيه العميل)                                                           |
| مغلّف الخطأ                                       | `AUTH_INVALID_CREDENTIALS` + `requestId` — **بلا Prisma أو مسارات أو أثر مكدّس** |

---

## 4. ما لم يُشغَّل — بصراحة

| البوابة                                              | الحالة      | السبب الدقيق                                                                                                                                             |
| ---------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| حزم المتصفح (Playwright)                             | **BLOCKED** | **0.6 غيغابايت حرة من 15.8** عند بلوغ هذه المرحلة، وسبق للـharness أن أوقف تشغيل Playwright لضغط الذاكرة. لم أُشغّل Chromium بدل إنتاج نتيجة غير موثوقة. |
| مصفوفة 320/390/430/768/1024/1440 × EN/AR × فاتح/داكن | **NOT_RUN** | تابعة للبند أعلاه                                                                                                                                        |
| تكبير 200% ولقطات before/after                       | **NOT_RUN** | تابعة للبند أعلاه                                                                                                                                        |
| axe / إتاحة آلية                                     | **NOT_RUN** | تابعة للبند أعلاه                                                                                                                                        |
| حزم `RUN_DB_INTEGRATION` للـAPI                      | **NOT_RUN** | لم تُشغَّل في هذه الجلسة؛ تحتاج خدمات حقيقية وذاكرة                                                                                                      |
| وظيفة CI `dispute-workspace`                         | **BLOCKED** | تحتاج **إذن نشر** — لم يُدفع أي commit                                                                                                                   |
| وظيفة CI `evidence-retention`                        | **BLOCKED** | نفس السبب                                                                                                                                                |
| استعادة نسخة احتياطية وكبت المحو                     | **NOT_RUN** | يحتاج بيئة تشغيلية مصرّحًا بها                                                                                                                           |
| قارئ شاشة / قبول بصري بشري                           | **BLOCKED** | يحتاج إنسانًا؛ لا يُنجزه فحص آلي                                                                                                                         |

**لا يوجد في هذا التقرير أي ادعاء بنجاح بوابة لم تُشغَّل.**

---

# ملحق — متابعة تطبيق الأدمن الموحد

**المصدر:** `6155a14` · **Draft PR:** #93 · **الأساس:** `8cad728`
**CI:** `35531480334` · **CodeQL:** `35531480187`

## 5. البوابات على المصدر النهائي (محليًا)

| البوابة                   | الأمر                                                                                                  |  rc | النتيجة                       |
| ------------------------- | ------------------------------------------------------------------------------------------------------ | --: | ----------------------------- |
| فحص أنواع الويب           | `pnpm --filter …/web typecheck`                                                                        |   0 | PASS                          |
| فحص أنواع E2E             | `pnpm --filter …/web typecheck:e2e`                                                                    |   0 | PASS                          |
| Lint الويب                | `pnpm --filter …/web lint`                                                                             |   0 | 0 أخطاء، **34 تحذيرًا** سابقة |
| بناء الويب للإنتاج        | `VITE_API_URL=… pnpm --filter …/web build`                                                             |   0 | PASS                          |
| **انحدار الأدمن الموسّع** | `vitest run …/disputes …/admin-provider-review …/admin-approvals …/admin-directory …/components/admin` |   0 | **251/251 في 37 ملفًا**       |
| تبويبات النزاعات          | `vitest run …/admin-workspace-tabs.test.tsx`                                                           |   0 | 12/12                         |
| **لوحة القيادة والترقيم** | `vitest run …/admin-dashboard-and-pagination.test.tsx`                                                 |   0 | **5/5**                       |

## 6. ما يثبته اختبار الترقيم — بحجم الصفحة الحقيقي

**حجم الصفحة 40 وليس 50.** `workspace.service.ts` يأخذ 41 صفًا ويقتطع 40،
بترتيب `[resolutionDueAt asc, disputeId asc]`. استخدمت الرقم الحقيقي للمستودع
لا رقمًا مستديرًا مخترعًا.

| السيناريو                                                                                                        | النتيجة                                         |
| ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| 41 سجلًا: الصفحة الأولى 40، ثم «تحميل المزيد» يجلب الحادي والأربعين                                              | PASS                                            |
| **كل صفوف الصفحة الأولى بنفس `resolutionDueAt`** — لو سقط فاصل `disputeId` لظهر صف مكرر أو مفقود عند حدّ المؤشّر | PASS — 41 صفًا **متمايزًا**، بلا تكرار وبلا فقد |
| المؤشّر المُرسَل هو آخر صف من الصفحة الأولى، لا إزاحة رقمية                                                      | PASS                                            |
| تغيير المُرشِّح **يُعيد بدء الترقيم** ولا يحمل المؤشّر القديم                                                    | PASS                                            |

## 7. عيب منتج اكتُشف أثناء كتابة الاختبارات

**السبب الجذري.** `ApprovalCenter.tsx` كان يقرأ `data?.items.length`. استجابة
صادقة (truthy) بلا حقل `items` ترمي `TypeError` **فتُسقِط لوحة الأدمن بالكامل**
إلى شاشة بيضاء بدل عرض الحالة الفارغة.

**الإصلاح.** `data?.items?.length` — محرف واحد يزيل مسار انهيار كامل.
**التغطية.** ظهر العيب عبر اختبار يغذّي استجابة ناقصة؛ صار مسار الخطأ الآن
مُمارَسًا في حزمة لوحة القيادة.

**ملاحظة نزاهة:** أول تشغيل لاختباراتي سقط لأن تثبيتة الاختبار نفسها كانت
`{}` غير صالحة، فانهار `DashboardOverview` أيضًا. أصلحت **التثبيتة** (استجابة
خطأ يتعامل معها المكوّنان بتصميمهما) ولم «أُحصّن» كل مكوّن ضد بيانات عشوائية.

## 8. ما زال غير مُشغَّل محليًا

| البند                               | الحالة            | السبب                                                                                                                                            |
| ----------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| متصفح Chromium محليًا               | **BLOCKED**       | 1.1 غيغابايت حرة من 15.8 عند البدء؛ `vmmemWSL` وحده 2 غيغابايت وChrome نحو 1، وليسا لي لأُنهيهما. لم أُشغّل Chromium بدل إنتاج نتيجة غير موثوقة. |
| مصفوفة 320→1440 × EN/AR × فاتح/داكن | **منقولة إلى CI** | وظائف PR #93 على `6155a14`                                                                                                                       |
| تكبير 200% حقيقي                    | **NOT_RUN**       | يحتاج متصفحًا                                                                                                                                    |
| قارئ شاشة / قبول بشري               | **BLOCKED**       | يحتاج إنسانًا                                                                                                                                    |

---

# ملحق 2 — ما كشفه CI على `0ccad07`، وكيف عولج

**CI:** `35531696963` — 13 وظيفة نجحت، **3 فشلت**. الفشل الثلاثي كله **انحدار
أحدثته أنا**، لا عيوبًا سابقة. هذا ما أثبت أن البوابات المحلية وحدها لم تكن كافية.

## 1. انحدار — تجاوز أفقي في لوحة الأدمن

```
Error: page scrolls horizontally by 14px (scrollWidth 404 vs clientWidth 390)
Error: page scrolls horizontally by  2px (scrollWidth 770 vs clientWidth 768)
```

**الوظيفة:** Browser E2E · **الاختبارات:** `admin-approvals.spec.ts`،
`admin-rtl.spec.ts` · **العرض:** 390 و768، بالإنجليزية والعربية، فاتح وداكن.

**السبب الجذري.** `DisputeCenterSummary` الذي أضفته وضع شبكة `.ac-metrics`
داخل `.ar-card` (حشو 24px على كل جانب)، بينما الشبكة مُصمَّمة لتكون **ابنًا
مباشرًا** للقسم. عند 768px تتحول الشبكة إلى **أربعة أعمدة**، فلم تتسع.
واستعملت صنفًا مخترعًا `.ac-header` لا وجود له في `approval-center.css` بدل
`.ac-hero` الذي يملك `display:flex; flex-wrap:wrap`.

**القرار النهائي: أُزيل المكوّن من لوحة القيادة.** السبب في §2.

## 2. انحدار — 403 في رحلة الأدمن الطبيعية

```
GET /v1/admin/dispute-workspaces → 403
expect(traffic.filter(status >= 400)).toEqual([])
```

**السبب الجذري.** طابور النزاعات يتطلب صلاحية `rolePermission` دقيقة قد لا
يملكها كل أدمن. مكوّن لوحة القيادة كان يستدعيه **لكل أدمن** عند كل زيارة، فيولّد
403 في رحلة طبيعية — أي أن اللوحة تستجوب نقطة نهاية قد لا يُسمح لها بها.

**لماذا لم أُعدِّل الاختبار.** التأكيد «لا استجابة ≥400 في رحلة الأدمن الطبيعية»
حارس جودة سليم، وتليينه كان سيخفي عيبًا حقيقيًا أحدثته.

**لماذا لم أُبقِ المكوّن.** لا توجد اليوم إشارة قدرة (capability) يرسلها الخادم
تخبر المتصفح بأن هذا الأدمن يملك قراءة النزاعات؛ و`/v1/admin/analytics/overview`
يحمل `disputesOpen` فقط وبنطاق صلاحية مختلف. جعل اللوحة تخمّن الصلاحية يخالف
«الخادم يملك الصلاحيات، والمتصفح ليس محرّك سياسات ثانيًا».

**الخلاصة الهندسية.** ملخّص النزاعات على لوحة القيادة **يحتاج عقدًا جديدًا**
يُصرّح بالقدرة من الخادم. أُزيل المكوّن، وسُجّل المطلوب بدقة في `HANDOFF.md`
بدل شحن لوحة تُصدر 403 عند كل فتح. **الهدف C يبقى جزئيًا، ولا يُدّعى إنجازه.**

## 3. انحدار — نصّ خاص مخفي خلف تبويب

```
Locator: getByText('...private original statement...')
Expected: visible · Received: hidden
```

**السبب الجذري.** رحلة المتصفح الحقيقية تؤكّد أن المراجع يرى بيان المشارك، وقد
صار ذلك البيان داخل تبويب «المعلومات» غير النشط — **مركّب لكن مخفي**، وهو
بالضبط السلوك المقصود من التبويبات.

**الإصلاح — قيادة الأداة الحقيقية لا كشف المخفي.** أضفت `openAdminTab(page, task)`
في `dispute-workspace-browser.cjs` يضغط زر التبويب الفعلي ويتحقّق من
`aria-selected="true"` وظهور اللوحة، ثم يؤكّد. يُستدعى مرّتين: قبل قراءة البيان،
وقبل «البتّ في الاستئناف» الذي صار في تبويب الاستئنافات.
**لم أحقن نمطًا ولم أكتب في DOM ولم أُضعِّف تأكيدًا.**

**ملاحظة أمنية محفوظة:** التأكيد أن المراجع الأصلي **لا يستطيع** البتّ في
الاستئناف (`toHaveCount(0)`) يعدّ العناصر المخفية أيضًا، فهو ما زال يثبت أن
**الخادم** يمنعه، لا أن تبويبًا أخفى الزر.

## 4. ما بقي أخضر في التشغيل نفسه

| الوظيفة                                                  | النتيجة |
| -------------------------------------------------------- | ------- |
| Install & lockfile · Contracts · Database · Web · API    | success |
| Integration & E2E (Postgres/Redis حقيقيان)               | success |
| **Evidence retention (Postgres + S3 versions + ClamAV)** | success |
| **Phase 5 visual, responsive and accessibility gate**    | success |
| Auth cookie contract (متصفح وAPI حقيقيان)                | success |
| Docker cold build + production boot · Compose smoke      | success |
| **Dependency, secret, and container scans**              | success |
| Phase 5 real-route and persistence evidence              | success |

---

# ملحق 3 — إصلاح الإتاحة في قائمة الوقائع

**التشغيل الذي كشفه:** CI `35533734443` على `db9f527` — **16 وظيفة، 15 نجحت**،
وفشلت وحدها «Dispute journey / real HTTP, browser, Postgres and ClamAV».
أي أن إصلاحَي الجولة السابقة (التجاوز الأفقي، والبيان المخفي) **نجحا**:
Browser E2E وAdmin review صارا أخضرين.

## 1. العدّ الدقيق للحزمة الفاشلة

```
Test Suites: 1 failed, 5 passed, 6 total
Tests:       1 failed, 30 passed, 31 total
```

الفشل الوحيد هو رحلة المتصفح الكاملة، وسببه **فحص الإتاحة** في نهايتها.

## 2. السبب الجذري

قاعدة axe `definition-list`، خطورة **serious**:

```
dl element has direct children that are not allowed: div > p
```

`<div>` داخل `<dl>` هو **مجموعة تعريف**، ولا يجوز أن تحوي سوى `<dt>` و`<dd>`.
سطرا «المصدر» و«سُجّل في» كانا **شقيقين** لـ`<dd>` لا داخله، فبطلت القائمة كلها.
تكرّر ذلك على كل واقعة، فبلغت الانتهاكات **79 عقدة**.

**الترميز أقدم من هذا الفرع.** كان داخل `<details>` مغلق افتراضيًا فلم يصل إليه
الماسح قط؛ ترقيته إلى لوحة ظاهرة هو ما كشفه.

## 3. الإصلاح — بلا كتم ولا إخفاء

نُقل السطران **داخل `<dd>`**، وهو أيضًا معناهما الصحيح: من أين جاءت الواقعة
ومتى سُجّلت جزءٌ مما يُعرَّف به المصطلح، لا محتوى منفصل يليه.

**لم يُعطَّل axe، ولم تُستثنَ القاعدة، ولم يُضف `aria-hidden`، ولم يُحذف أي
مصدر أو مُعرِّف أو تاريخ.**

وأُعيد الصنف `cw-facts` إلى القسم، فقواعد
`.cw-facts dd { margin-inline: 0 }` وفاصل المجموعات كُتبت للغلاف القديم
`<details class="… cw-facts">` وتوقّفت عن العمل حين نُقل الترميز — انحدار بصري
أهدأ من الأول عالجته الآن، وبه يبقى المظهر كما كان بعد نقل السطرين.

## 4. تغطية الانحدار — مُثبَتة بالطفرة

`workspace-overview-semantics.test.tsx` — **5 اختبارات** تُرمِّز قاعدة axe بنيويًا:
كل ابن مباشر لـ`<dl>` من الأنواع المسموحة؛ كل مجموعة فيها `dt` واحد و`dd` واحد؛
القيمة **وسطرا المصدر والتاريخ** داخل `<dd>`؛ لا `aria-hidden` على أي منها؛
والعربية لا تفقد المصدر.

**اختبار الطفرة:** بإعادة سطر «سُجّل في» خارج `<dd>` **تسقط 3 من 5**. الحارس حقيقي.

## 5. المقابض المفتوحة (Task 3)

فحصت سجل الوظيفة: **لا يوجد أي تحذير open-handle**، ولا «did not exit»، ولا
«force exited». التحذيرات الموجودة كلها بيئية: تخطيط pnpm، إهمال
`isolatedModules` في ts-jest، تنبيه AWS SDK بخصوص Node ≥ 22، وملاحظات حاويات
Postgres/Redis. **لا شيء يُغلق، ولم أستخدم `--forceExit`.**

## 6. بوابات محلية على المصدر الجديد

| البوابة                       |    rc | النتيجة                         |
| ----------------------------- | ----: | ------------------------------- |
| `git diff --check`            |     0 | نظيف                            |
| web typecheck / typecheck:e2e | 0 / 0 | PASS                            |
| web lint                      |     0 | 0 أخطاء، 34 تحذيرًا (خط الأساس) |
| انحدار النزاعات والأدمن       |     0 | **241/241 في 36 ملفًا**         |
| دلالات لوحة الوقائع (جديد)    |     0 | **5/5**                         |
| web production build          |     0 | PASS                            |
| api typecheck                 |     0 | PASS                            |

**لم تُشغَّل حزمة `dispute-workspace-browser` محليًا** — تحتاج
Postgres/Redis/ClamAV ومتصفحًا حقيقيًا، والذاكرة على هذا الجهاز لا تسمح.
**لا أدّعي نجاحًا محليًا لم يُنفَّذ**؛ الإثبات من CI على الـSHA النهائي.
