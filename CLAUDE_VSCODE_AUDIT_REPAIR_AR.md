# برومت تدقيق وإصلاح HomeServiceMarketplace داخل Claude Code / VS Code

## المهمة وحدودها

اعمل كمهندس Full-Stack وDevSecOps ومدقق جودة برمجيات. افحص المشروع فعليًا، وأصلح عيوب التشغيل والتنفيذ المثبتة، واختبر النتائج. لا تكتفِ بخطة أو مراجعة سطحية، ولا تعد بناء ميزات تعمل أصلًا.

المستودع: `https://github.com/baraabd/homeservicemarketplace`
المجلد المتوقع: `C:\Users\mohab\Documents\GitHub\Homeservicesmarketplace`
تحقق من جذر مساحة العمل الفعلية بدل افتراض المسار.

النطاق: تجربة العميل Seeker، والمهني Provider والتسجيل V2، والأدمن Admin، والمصادقة والجلسات، والطلبات والعروض والحجوزات، والمحادثات والإشعارات والنزاعات، والوثائق والوسائط، والماليات، وAPI والعقود وقاعدة البيانات، وDocker وCI وإعدادات النشر.

التعديلات المحلية اللازمة لهذه الإصلاحات وتشغيل الاختبارات المعزولة هي هدف المهمة. احترم موافقات أدوات Claude Code. لا تنفذ commit أو push أو merge إلى فرع بعيد، ولا تغيّر حماية الفروع، ولا تنشر إلى الإنتاج، ولا تشغّل معاملات مالية حقيقية دون موافقة صريحة منفصلة. إكمال merge محلي جارٍ يحتاج مراجعة ما سيدخل معه من تعديلات سابقة وموافقة المستخدم؛ لا تثبّت عمله كله تلقائيًا.

استخدم Mode A للإصلاح والتكامل. يسمح بإصلاح UX والإتاحة المرتبط مباشرة بالعيب؛ لا تحول التدقيق إلى إعادة تصميم شاملة أو ترحيل معماري أو إعادة ترقيم السبرينتات. تواصل بالعربية، وأبقِ أسماء الكود والاختبارات وفق اصطلاحات المشروع.

## معلومات البداية: أعد التحقق ولا تعاملها كحقيقة دائمة

لقطة المراجعة الخارجية بتاريخ 18 سبتمبر 2026:

- `develop` عند commit `43bc876b4115661f3b06279e278e89c713cc6315`، وبه دمج PR #85 لسبرينت 12.
- CI run `35370649883` فاشل عند API typecheck؛ CodeQL run `35370649677` ناجح.
- أخطاء TS2305 في `money-application.ts` و`money-domain.ts`: الأنواع BillingInterval وPaymentMethod وEntitlementDecision وLedgerEntryContract غير مصدّرة من نقطة دخول حزمة contracts. تعريفاتها موجودة في `packages/contracts/src/money/index.ts`؛ لكن `packages/contracts/src/index.ts` لا يصدّرها.
- PR #87 دُمج سابقًا، وأزال ترحيلات مالية غير مكتملة مع الإبقاء على domain/contracts. لا تفترض أن عائق Prisma السابق هو سبب الفشل الحالي، ولا أن إزالة تلك الترحيلات من Git تعني عدم تطبيقها على قواعد بيانات محلية.
- اجتاز الويب فحوصه الأساسية في التشغيل المذكور. اختبارات API وبناؤه بعد typecheck، وعشر مهام تشغيل/تكامل/متصفح/أمان تابعة، لم تنفذ. ليست نتائج نجاح.

آخر حالة محلية أرسلها المستخدم، وقد تكون تغيرت:

- الفرع `feat/provider-onboarding-v2-phase5-exact-visual-parity`.
- اختلاف 4 commits محلية و5 في مرجع التتبع، مع merge جارٍ وتعديلات كثيرة staged.
- ثلاثة تعارضات: ملف اختبارات الأسواق both added، وخدمة الأسواق ومنسق الحفظ both modified:
  `apps/api/src/modules/provider/onboarding/market/supported-markets.service.spec.ts`
  `apps/api/src/modules/provider/onboarding/market/supported-markets.service.ts`
  `apps/web/src/app/features/provider-onboarding-v2/autosave/ProviderOnboardingAutosaveProvider.tsx`
- سجل Vite أظهر علامة `<<<<<<< HEAD` في منسق الحفظ عند السطر 660. لا تزل النافذة التحذيرية بدل إصلاح التعارض.

## المرحلة 0 — حماية العمل المحلي وتثبيت خط الأساس

ابدأ بقراءة حالة Git والأدوات والعمليات، دون تعديل:
`git status --short --branch`
`git branch --show-current`
`git rev-parse HEAD`
`git rev-parse -q --verify MERGE_HEAD`
`git ls-files -u`
`git diff --stat`
`git diff --cached --stat`
`git stash list`
`git worktree list`

غياب MERGE_HEAD نتيجة متوقعة إذا لم يعد هناك دمج، لا خطأ يحتاج افتعال دمج لمعالجته.

سجل HEAD وMERGE_HEAD وupstream والفروق الحالية. احفظ نسخة محلية آمنة خارج المستودع من الملفات المعدلة، والملفات غير المتتبعة اللازمة، وpatches، ومعلومات الفهرس. عند التعارض احفظ نسخ base وours وtheirs حيث توجد؛ لا تتوقع base لملف both added. احفظ البايتات والعربية ونهايات الأسطر، لا تعتمد على تحويل PowerShell قد يغير الترميز. لا ترفع النسخ الاحتياطية أو الأسرار ضمن التقارير.

لا تستخدم reset --hard أو clean -fd أو restore شاملًا أو stash عشوائيًا أو force-push. لا تستخدم merge --abort ولا pull/rebase/دمجًا ثانيًا أثناء الدمج الجاري. يمكن تحديث مراجع المقارنة بـfetch بعد حفظ الحالة وبموافقة الأداة، دون تغيير HEAD أو MERGE_HEAD أو ملفات المستخدم.

إن بقيت التعارضات، قارن الأطراف الثلاثة وسجل سبب كل قرار. احتفظ بإصلاحات فشل الحفظ والتزامن واكتمال الأسواق واختباراتها. لا تستخدم قبول ours/theirs/both بصورة شاملة، ولا تستبدل الملفات بنسخ develop كاملة. افحص أيضًا الملفات المدمجة آليًا، خصوصًا تصدير العقود والـschema والإعدادات المشتركة. أزل العلامات بعد اتخاذ القرار الدلالي، ثم افحص diff وtypecheck واختبارات النطاق. لا تستخدم git add -A. اطلب موافقة المستخدم قبل تجهيز ملفات حل التعارض أو تثبيت merge يضم عمله السابق.

إذا تعذر تبديل الفرع بأمان، تابع التدقيق في شجرة عمل معزولة مع توثيق مرجعها؛ لا تنسب نتائجها لنسخة المستخدم ولا تفقد commits غير المدفوعة. لا تشغّل وكيلين يكتبان في الملفات نفسها.

## المرحلة 1 — جرد حقيقي للمشروع

اقرأ `CLAUDE.md` و`.claude/rules/ux-ui-design-policy.md` والقواعد ذات الصلة، وpackage.json لكل workspace، وملفات CI وDocker، والعقود وADRs ووثائق السبرينتات الحديثة. افصل التعليمات الحالية عن تقارير الإنجاز التاريخية.

استخرج قائمة التطبيقات والحزم والمسارات والـcontrollers والـworkers والـfeature flags. لكل رحلة رئيسية تتبع:
Route → Guard → Component → Hook/API client → Contract/DTO → Controller → Service → Repository → Schema/migration → Notification/outbox/audit.

أنشئ مصفوفة تبين: الموجود والمرتبط فعليًا، الموجود لكنه معطل بعلم، الموجود دون ربط، البيانات الوهمية، الوظائف المؤجلة، وما لم تفحصه بعد. نتيجة بحث فارغة واحدة لا تثبت غياب الميزة. لا تعتبر وجود ملف أو زر دليلًا على اكتمالها.

## المرحلة 2 — استعادة البناء قبل توسيع التطوير

أعد إنتاج خطأ تصدير الأنواع على اللقطة المعنية أو سجل أنه عولج في مرجع أحدث. أصلح نقطة دخول `packages/contracts/src/index.ts` وفق اصطلاح تصدير الأنواع والقيم في المشروع، مع إبقاء تصديرات disputes وبقية الحزم. افحص جميع مستهلكي money، وليس الأنواع الأربعة فقط. أضف فحصًا يثبت الاستيراد من نقطة الدخول العامة ويكشف فقدانه بعد دمج آخر.

لا تكرر تعريف الأنواع داخل API، ولا تستعمل any أو ts-ignore أو تستبعد ملفات money من TypeScript. أعد بناء العقود ثم API والويب وتحقق من الاستيرادات وقت البناء والإنتاج. إصلاح التصدير لا يعني أن نظام الدفع أصبح مكتملًا.

تحقق من pnpm المثبت بإصدار packageManager واستخدم frozen lockfile. اقرأ scripts الحالية قبل تشغيلها؛ من الأوامر الموجودة في لقطة المراجعة:
`pnpm --filter @homeservicemarketplace/contracts build`
`pnpm --filter @homeservicemarketplace/api typecheck`
`pnpm --filter @homeservicemarketplace/web typecheck`
`pnpm --filter @homeservicemarketplace/web typecheck:e2e`

نفذ توليد Prisma وبناء حزمة database عند الحاجة داخل الإعداد المعزول الصحيح. لا تضع DATABASE_URL وهميًا فوق .env المستخدم. عالج أول فشل بسبب جذري واختبار regression، ثم أعد الفحوص؛ لا تبدأ ميزات جديدة قبل استقرار الأساس.

## المرحلة 3 — سجل العيوب والإصلاحات المحددة

لكل عيب سجل: المعرف، والأولوية، والدليل، والمرجع، والملف والرمز، والمسار المتأثر، وشروط ظهوره، وخطوات إعادة الإنتاج، والمتوقع مقابل الفعلي، والإصلاح الأدنى، والاختبار، والنتيجة. صنفه كـCONFIRMED أو CONDITIONAL أو HYPOTHESIS أو INCOMPLETE_FEATURE، ثم FIXED_VERIFIED أو OPEN أو BLOCKED بالأدلة.

أعد التحقق خصوصًا من النتائج التالية:

1. العميل: في `HomeScreen.tsx` تحوّل statusKeyFor الحالة CANCELLED إلى completed ويعرضها الكارت بمسمى done. احتفظ بمعنى الإلغاء مستقلًا، وأضف اختبارًا لكل حالة باللغتين. لا تغير حالة الخادم لمجاراة خطأ العرض.

2. الأسعار: BookingListItem والخادم يرسلان currency وpricingType، لكن apiBookingToItem لا يحتفظ بهما وتعرض البطاقة الدولار و/hr حرفيًا. أصلح mapper والعرض باستخدام العقد الفعلي. تتبع معنى priceAmount ووحداته عبر إدخال العرض والتخزين والإرجاع قبل اختيار أي تحويل. لا تفترض أن كل مبلغ cents أو كل خدمة بالساعة. اختبر سعرًا ثابتًا وسعرًا ساعيًّا وعملة غير USD وحالات الصفر والقيم الناقصة.

3. واجهة العميل: الأرقام 4.9★ و500+ و~1h ثابتة داخل HomeScreen، وزر View all للخدمات لا يملك إجراءً. اربط الأرقام بمصدر معتمد أو أزل ادعاء القياس من واجهة التشغيل. اربط الزر بوظيفة ذات معنى أو لا تعرضه كإجراء فعال. لا تحول قيمًا وهمية إلى استجابة API وهمية.

4. إشعارات المهني: `ProviderNotifications.tsx` يعلّم العنصر كمقروء فقط. افحص ربطه بمحلل notification-target ومسارات العمل. المحلل يولد مسارات مثل /provider/bookings/:id و/provider/chat/:id بينما الموجّه الحالي يستخدم /provider/messages/:threadId ولا يعرّف جميع مسارات التفاصيل القديمة. أثبت المسار الصحيح من نقطة النقر والفتح المباشر وإعادة التحميل وتسجيل الدخول. حافظ على التفويض وملكية المورد؛ لا تصلح الرابط بإرسال الجميع للصفحة الرئيسية.

5. ماليات الأدمن والمهني: `booking.repository.ts` يجمع priceAmount دون فصل العملة، ثم تستخدم الخدمات dominantCurrency. هذا خطأ عند وجود عدة عملات. اختبر بيانات مصطنعة بعملتين، وافصل التجميع بحسب العملة أو أثبت قيد عملة واحدة مفروضًا عند كل كاتب إذا كان ذلك قرار المنتج. لا تجمع عملات مختلفة تحت تسمية واحدة ولا تخترع سعر صرف.

6. التقريب والعمولة: `admin-financials.service.ts` يقرب عمولة المجموع في summary ويقرب كل حجز في listBookings. مثال اختبار: مبلغـان 104 و104، ونسبة 1500 basis points؛ مجموع تقريب الصفوف 32، وتقريب الإجمالي 31. حدد مصدر الحقيقة والسياسة ثم وحّد المجاميع والصفوف والرسوم. افحص أيضًا إعادة احتساب الماضي بقيمة العمولة الحالية، وtotalRefunds الثابت، ومعنى availableBalance. ميّز نموذج قراءة الأرباح عن دفتر مدفوعات فعلية، ولا تختلق تاريخ تحصيل أو استرداد.

7. التخزين: Compose الحالي يثبت `/var/lib/hsm/media` فقط، بينما local restricted adapter يستخدم RESTRICTED_STORAGE_DIR أو `<cwd>/.restricted-uploads`. تحقق من صلاحية الكتابة وبقاء الوثائق الخاصة بعد إعادة إنشاء الحاوية المعزولة. أصلح المسار والحجم الدائم المنفصل والصلاحيات غير الجذرية. لا تضع وثائق الهوية داخل الجذر العام لحل مشكلة الكتابة.

8. money يحتوي أساس domain/contracts؛ لا تفترض وجود API أو دفع مكتمل لمجرد وجوده. افحص ربط DI وports والتخزين عند توصيله. حقول bigint في العقود تحتاج تمثيلًا صالحًا لـJSON عند حدود HTTP؛ لا تسجل هذا كعطل 500 مؤكد قبل وجود مسار فعلي وإعادة إنتاجه. السحب البنكي في WalletScreen معطل عمدًا؛ لا تضف نجاحًا وهميًا ولا تفعّل مالًا حقيقيًا ضمن هذه المهمة.

9. نسخة Node20 مثبتة في المشروع وفي CI؛ تحقق من جدول الدعم الرسمي وخطط لترقية مدعومة مثل Node24 LTS في تغيير مستقل بعد تثبيت baseline. وحّد التطوير وengines وvolta وDocker وCI والتوافق مع Prisma وأدوات البناء. لا تحدث جميع الحزم إلى latest ولا تعتبر ترقية Node علاجًا تلقائيًا لفقد التصديرات.

10. حماية develop: راجع الحماية والقواعد بأذونات قراءة فقط. سجل أسماء الفحوص المطلوبة الفعلية وتوصية فرضها. عدم القدرة على قراءة إعداد لا يعني أنه غير موجود. لا تغيّر حماية GitHub أو تتجاوزها تلقائيًا.

لا تنحصر بهذه القائمة: افحص بقية المسارات، وTODOs المتصلة بالإنتاج، والتوابع الفارغة، والنجاح المؤقت الوهمي، والأزرار غير المرتبطة، والاختلاف بين DTO والواجهة؛ أضف عيوبًا جديدة فقط بأدلة.

## المرحلة 4 — قاعدة البيانات وبيئة الاختبار

قبل أي ترحيل حدد اسم قاعدة البيانات ومضيفها ونوع البيئة دون طباعة كلمات المرور. استخدم قواعد بيانات وأحجام تخزين وبيانات اختبار منفصلة. لا تفحص بإجراءات كتابة على الإنتاج أو بيانات المستخدم الفعلية.

قارن migrations وschema.prisma وgenerated client وسجل `_prisma_migrations`. اختبر إنشاء قاعدة نظيفة وترقية نسخة اختبار من الحالة السابقة. افحص constraints والفهارس وbackfill والتزامن، بما يشمل قيود SQL التي لا يعبّر عنها Prisma.

إذا وجدت ترحيلات محذوفة من Git لكنها مطبقة، توقف عن تغييرات schema لذلك الهدف، ووثق الخطة المتوافقة مع البيانات الموجودة. لا تحذف سجل الترحيلات، ولا تعيد كتابة ترحيل مطبق، ولا تستخدم migrate reset أو db push --accept-data-loss أو حذف volumes. النسخ الاحتياطي لا يصبح دليل استعادة إلا بعد تجربة restore منفصلة.

جهز Postgres وRedis وMailpit ومخزن اختبار وفاحص ClamAV حقيقي للبوابات التي تتطلبه، وفق scripts المشروع. الأسماء والمنافذ في Compose ثابتة؛ اسم مشروع جديد وحده لا يضمن العزل. استخدم إعداد اختبار منفصلًا بلا تعارض أسماء/منافذ/volumes. سجل الحاويات والعمليات التي أنشأتها ونظفها وحدها. لا توقف Prisma Studio أو حاويات المستخدم أو عملياته لحل قفل ملف دون موافقته.

## المرحلة 5 — إثبات الرحلات الحقيقية لكل التطبيقات

نفذ الاختبارات بوصف كل منها الحقيقي والمُحاكى بوضوح. اختبارات browser acceptance الأساسية تتصل بـAPI وقاعدة بيانات فعليين؛ mocks المنطقية تبقى في اختبارات منفصلة. يمكن محاكاة إذن GPS بصورة معلنة، لكن ذلك لا يثبت حفظ الإحداثيات إذا استبدلت API التطبيق أيضًا.

العميل: التسجيل والتحقق واستعادة كلمة المرور، والعنوان والموقع، وإنشاء طلب وصور، وعرض العروض وقبول واحد، والحجز والإلغاء والتفاصيل والسجل، والمحادثة، والإشعارات، والنزاع. اختبر الطلب المتكرر والتزامن وفشل الشبكة والأسعار والحالات.

المهني: التفعيل والمهام الست، والحفظ الفوري ثم مغادرة الشاشة، وتعديل خطوتين بسرعة، و409 وتبويبين، ورفض الحفظ وفشل رفع الصور، وإعادة التحميل والدخول بحساب آخر. لا تعرض Saved قبل إقرار الخادم. اختبر الهاتف والخريطة ورفض الإذن والنبذة وترتيب الصور وساعات العمل والمنطقة الزمنية والتقديم وطلب التعديل وإعادة الإرسال.

اختبر أهلية العمل من الخادم لا من حالة ACTIVE أو إخفاء navigation فقط: ناقص التسجيل، وتحت المراجعة، ومرفوض، وموقوف، وتصريح منتهي أو مسحوب. أعد التجربة عبر API وروابط مباشرة وجلسة جديدة وتجديد token. حافظ على وصول صاحب الحجز السابق للدعم/النزاع حيث تقرره السياسة، دون منحه حق عمل جديد.

الأدمن: الدخول الصحيح ورفض غير المخول، ودليل المستخدمين والمهنيين وطوابير المراجعة، والمهام الست والوثائق الخاصة والتخصصات، وطلب التعديل والقبول والرفض وسحب التصريح. اختبر تنافس قرارين، وعدم التكرار، وrollback عند فشل جزء، وتسجيل audit/outbox، وتحديث واجهة المهني. لا تفترض أن دور admin يمنح كل صلاحيات قراءة الوثائق. افحص الإعدادات والماليات والمراجعة والنزاعات والتنقل المباشر.

التواصل والوسائط: ملكية المحادثة والمرفقات، وإعادة الاتصال واستعادة الحالة، وعدادات القراءة، وتسليم outbox وإعادة المحاولة دون تكرار الأثر. افحص الفصل بين public وrestricted، وعدم تسرب الوثائق أو المفاتيح أو بيانات الهوية إلى logs/URLs. اختبر الفحص الحقيقي وحالات pending/failure/clean والاحتفاظ والحجز القانوني متى كان موجودًا والحذف المؤكد من التخزين؛ لا تعتبر حذف صف مساويًا لحذف كل نسخة S3/CDN.

لا تستخدم حسابات أو هويات أو ملفات خاصة حقيقية؛ استخدم fixtures اصطناعية ومسارات اختبار معزولة.

## المرحلة 6 — الفحوص والبناء والقبول المرئي

اقرأ أسماء مهام CI وشروطها من النسخة الحالية، وشغّل تسلسلها المحلي الممكن، لا قائمة قديمة عمياء. تحقق من lint/typecheck/unit/build للعقود وقاعدة البيانات وAPI والويب، وtypecheck لاختبارات E2E، ثم integration على Postgres/Redis وbrowser E2E وauth-cookie وreal-route persistence للمهني والأدمن، وretention مع التخزين والفاحص الحقيقي، وDocker cold build وproduction boot وCompose smoke والفحوص الأمنية المتاحة.

لا تشغّل في PowerShell أوامر Bash كأنها صالحة؛ استخدم البيئة المدعومة المتاحة وسجلها. تحقق من exit code لكل برنامج خارجي، حتى عند حفظ مخرجاته عبر pipeline. حافظ على UTF-8 ولا تغير نهايات الأسطر جماعيًا.

لواجهات تغيرت: اختبر 320 و390 و430 و768 و1024 و1440، والعربية/RTL والإنجليزية، والفاتح والداكن. افحص اللمس والتركيز ولوحة المفاتيح وحالات empty/loading/error/offline والتباين. التقط صورًا من التطبيق الحقيقي ووثق المسار واللغة والعلم والبيانات والمرجع. لا تولد صورًا تصميمية وتقدمها كصور قبول، ولا تغيّر baseline أو ميزانية الفروق لإخفاء فشل.

سجل أعداد passed/failed/skipped لكل بوابة وسبب كل skip. NOT_RUN وBLOCKED وSKIPPED ليست PASS. إذا كانت أداة أو خدمة غير متاحة، سجل عائقها وأكمل العمل الآمن المستقل؛ لا تستبدل الاختبار الحقيقي بمحاكاة ثم تسميه ناجحًا.

## المرحلة 7 — جاهزية التشغيل دون نشر

راجع إعدادات build-time مقابل runtime، خاصة VITE_PROVIDER_ONBOARDING_V2 وإعدادات VITE_API_URL. افحص صلاحيات العمل والتحقق وإعدادات ماسح الوثائق والـworkers واحتفاظ الوسائط. لا تبدل feature flags الأمنية لتجاوز فشل اختبار، ولا تسمح لـtest scanner بالعمل في الإنتاج.

راجع الدومينات وTLS وCORS والكوكيز وCSRF والبريد والتخزين والأسرار والـhealth/readiness والمراقبة والنسخ الاحتياطي والاستعادة وخطة rollback. لا تعتبر Compose التطوير ملف نشر إنتاجي، ولا غياب ملف نشر داخل المستودع دليلًا على عدم وجود استضافة خارجه. سجل ما أمكن إثباته وما يحتاج صلاحيات أو بيانات تشغيل غير متاحة.

لا تختَر بوابة دفع أو سياسة عمولة أو سوق إطلاق جديد نيابة عن المستخدم. الإصلاحات البنيوية المقبولة تختلف عن قرارات المنتج أو التعامل مع أموال حقيقية.

## قواعد لا يجوز تجاوزها أثناء الإصلاح

لا حذف لاختبار فاشل أو تعطيله لمجرد إنجاح CI، ولا continue-on-error لبوابة إلزامية، ولا تقليل متطلبات الإتاحة أو التحايل على branch protection. تعديل اختبار خاطئ مسموح فقط بعد دليل موثق مع إبقاء عقد القبول.
لا تعطيل auth/ownership/CSRF/rate limiting ولا مزج الوثائق الخاصة بالصور العامة، ولا تسجيل أسرار أو tokens أو هوية المستخدم.
لا استبدال مصدر بيانات حقيقي بوهمي، ولا دفعات أو سحب وهمي، ولا نسخ قواعد التفويض إلى الواجهة بدل الخادم.
لا refactor شامل أو migration إلى microservices أو إعادة تصميم غير مرتبطة بالعيوب. حسّن حدود الملفات الكبيرة فقط حيث يخدم ذلك الإصلاح واختباراته.

## المخرجات المطلوبة ونقطة التوقف الصادقة

أنشئ تحت `docs/audit/<تاريخ-التنفيذ>/`:

- AUDIT.md: النطاق الفعلي ومصفوفة التطبيقات والمسارات والملفات التي لم تُفحص.
- DEFECTS.md: سجل العيوب والأدلة والإصلاحات والحالات.
- TEST_EVIDENCE.md: البيئة والأوامر وأكواد الخروج والنتائج والصور ومرجع التنفيذ.
- REPAIR_REPORT.md: الملفات المعدلة وأسبابها والمخاطر والإعدادات وخطة التراجع وما يحتاج موافقة.
- CONTINUATION.md: آخر نقطة مكتملة والخطوة التالية الدقيقة والمهام المحجوبة.

إذا كانت الشجرة غير مثبتة، اذكر HEAD مع حالة dirty وملخص/بصمة diff، لا تنسب اختبارات التعديلات إلى commit لا يحتويها. افصل نتائج شجرة إصلاح معزولة عن نتائج شجرة المستخدم وعن CI البعيد. أعد الفحوص المتأثرة بعد آخر تعديل.

في النهاية قدم: ما أُصلح وثبت، وما بقي، وما لم يمكن اختباره، وحالة كل بوابة، والقرار بخصوص جاهزية البناء والتكامل والنشر كلًا على حدة. لا تقل إن develop البعيد أصبح أخضر دون تشغيل ناجح فعلي للمرجع النهائي بعد دفع مصرح به. لا تقل 100% آمن أو خالٍ من الأخطاء.

ابدأ الآن بالجرد وحماية حالة Git، ثم أصلح العوائق القابلة للإثبات بالتتابع. حدّث نقطة الاستكمال عند اكتمال كل مرحلة، وتوقف فقط عن الخطوة التي تحتاج موافقة أو وصولًا غير متاح مع توضيحها؛ لا تتجاوز وسائل الحماية ولا تختلق نتيجة نجاح.
